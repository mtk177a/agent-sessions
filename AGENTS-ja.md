# AGENTS.md

> **注記:** [英語版](AGENTS.md) が正式版です。
> この日本語版は参考訳です。
> 内容に差異がある場合は英語版を優先します。
> このファイルは独立したエージェント向け指示や権限を定めません。

英語版は、`agent-sessions` で作業するエージェント向けの、このリポジトリに固有の指示を定めます。

## 目的

`agent-sessions` は、複数のプロバイダーが記録したコーディングエージェントのセッションを、ローカルで読み取り専用で参照するための公開ツールです。

このリポジトリは、プロバイダーへのアクセス、正規化、検証、公開仕様を担います。\
利用側に固有の解釈、振り返りによる所見、個人的な文脈、処理状態は扱いません。

## 最初に読む資料

設計や実装に重要な変更を加える前に、次の資料を読みます。

1. [`README.md`](README.md)
2. [`docs/architecture.md`](docs/architecture.md)
3. 変更に直接関係する ADR

さらに、次の規則に従います。

- 網羅することだけを目的に、無関係な文書まで調べない。
- プロバイダーの動作が設計に大きく影響する場合は、過去の前提に頼らず、現行の公式文書または実装を確認する。

## 公開情報の範囲

これは公開リポジトリです。

次の情報を追加してはいけません。

- 非公開リポジトリの名前やリンク
- 非公開の Issue や Project への参照
- 個人や雇用先に固有の事情
- 実際のコーディングエージェントのセッション内容
- 認証情報
- 実在するユーザー名、ホスト名、ホームディレクトリ、マシン固有のパス
- 非公開の利用側実装の詳細

非公開または特定の環境での利用から分かった要件は、それ自体で成立する一般的な製品要件に言い換えた場合に限り取り込めます。\
たとえば、独立した複数の Codex ソースインスタンスを名前で区別するという要件は有効です。\
その要件が明らかになった非公開システムやマシンを記述してはいけません。

## アーキテクチャの主要な制約

明示的な設計判断によって変更されない限り、次を維持します。

- プロバイダーが保持する対話記録を元データとする。
- 読み取り専用の操作では、プロバイダーのデータやメタデータを変更しない。
- 読み取り専用の操作では、`agent-sessions` のデータを永続的に書き込まない。
- 既定では、ツールは過去の実行結果に意味上依存しない。
- 利用側の処理状態を `agent-sessions` に置かない。
- 対話内容全体を `agent-sessions` のデータストアに複製しない。
- デーモン、監視処理、タイマー、定期的な同期処理を必要としない。
- 同じプロバイダーの複数の保存場所は、名前付きソースインスタンスとして区別する。
- ファイルシステムのパスは位置を示すものであり、安定した論理 ID ではない。
- プロバイダー内部の保存形式に関する知識は、通常の利用者設定ではなくプロバイダーアダプターに置く。
- 観測が不完全な状態、未対応の状態、失敗した状態を区別できるようにする。

将来役立つ可能性だけを理由に、データベース、永続的な索引、バックグラウンド処理、フックによる取り込み経路、対話内容の複製を導入してはいけません。\
導入には、実際に確認された必要性、測定結果、関係する設計判断の見直しが必要です。

## ディスクと資源の使用

SSD への書き込みによる寿命への影響は、明示的な設計上の関心事です。

ディスクと資源の使用について、次を守ります。

- 読み取り専用コマンドに永続的な書き込みを追加しない。
- 必要性を示す測定結果がないまま、対話的な応答を速くするために、履歴全体の定期的な走査や高頻度の状態更新を追加しない。
- 将来キャッシュを導入する場合も、削除可能で、正式なデータ源として扱わない。\
  削除によって変わるのは性能だけでなければならない。
- 変更が永続的な入出力に影響する場合は、アプリケーションが扱うデータ量から安全性を推測せず、書き込み経路を直接確認する。

## プロバイダーへのアクセス

プロバイダーへのアクセスでは、次を守ります。

- 読み取り専用というこのリポジトリの要件を満たす場合は、プロバイダーが文書化したインターフェースを優先する。
- 公式インターフェースであることだけを、安全性の根拠にしない。
- 公式の読み取り操作がメタデータの修復、移行、同期、その他の永続的な変更を行う場合は、より安全で対応済みの経路を検討する。
- プロバイダー固有の解析処理は、プロバイダーごとの責務範囲に閉じ込める。
- プロバイダー内部の不安定な構造を、明示的な互換性の判断なしに安定した公開仕様として示さない。

## テスト用データ

テスト用データについて、次を守ります。

- 合成したデータだけを使用する。
- 合成データは最小限としつつ、必要に応じて不正な入力や攻撃的な入力も含め、対象の動作を検証できるようにする。
- 実際の非公開セッションを伏せ字にして、コミットするテスト用データを作らない。\
  元から独立して構築する。
- 認証情報に見えるテスト用の値には、明らかに架空と分かる値を使う。

## 利用側との責務分担

次のような領域固有の概念を追加してはいけません。

- セッションがレビュー済みかどうか
- 挙動の良し悪し
- 振り返りによる所見や提案
- 個人の記憶に関する分類
- 後続の作業の状態

このアクセス層が公開するのは、ソース ID、ソースのバージョン、正規化された観測結果、完全性、検証結果です。\
その観測結果にどのような意味を与えるかは、利用側が決めます。

## 言語

公開文書とリポジトリ規則は、英語版を正式版とします。\
理解とレビューのため、[言語方針](docs/localization.md) に列挙された日本語参考訳を保守します。\
日本語版は独立した要件や権限を定めません。\
内容に差異がある場合は英語版を優先します。

英語を既定とする対象は次のとおりです。

- コードと識別子
- コードコメント
- README と文書
- ADR
- CLI のヘルプと診断メッセージ
- コミットメッセージの要約
- リリースノート
- リポジトリの規則ファイル

保守対象の英語版を追加または変更する場合は、リポジトリ専用の `maintain-japanese-references` Skill を使って対応する日本語版を確認します。\
意味が変わる場合は、同じ変更内で参考訳を作成または更新します。\
意味が変わらない場合は参考訳を変更せず、その理由を Pull Request の「検証」または「リスク / フォローアップ」に記録します。\
`docs/decisions/` 配下の ADR の翻訳は、保守対象の参考訳に含めません。

保守者の作業では、レビューや意思決定の負担を減らす場合に日本語を使用できます。

保守者が作成する公開 Issue と Pull Request では、次を守ります。

- タイトルを英語にする。
- 公開された変更を識別できる短い英語の `Summary` を含める。
- その他の本文とコメントは日本語を既定とし、必要に応じて英語も使用できる。
- 本文全体を英語と日本語の両方で記載しない。

外部のコントリビューターに、保守者向けの日本語による運用は要求しません。

## 変更時の規律

現在の要件を十分に満たし、公開仕様を維持できる最小の変更を優先します。\
具体的な要件なしに、対応プロバイダー、互換性処理、キャッシュ、索引、バックグラウンド処理、設定項目を増やしてはいけません。

アーキテクチャの不変条件を変更するときは、次を行います。

1. 影響を受ける設計判断を特定する。
2. 既存の ADR が現在も有効か判断する。
3. 判断が変わる場合は ADR を更新または置き換える。
4. `README.md`、アーキテクチャ文書、実装、テストを、採用した状態と整合させる。

現在の設計判断を説明するために必要な場合を除き、現行の文書に実装の経緯を追加してはいけません。

## 検証

影響を受ける要件を確認できる、最も範囲の狭い再現可能な検査を実行します。

- 実装が追加されたら、作業ごとにコマンドを考案するのではなく、このファイルまたは専用のコントリビューター向け文書に、リポジトリの標準的な検証コマンドを記載する。
- 完了を報告する前に、最終差分に非公開情報やマシン固有のパスが誤って含まれていないか、必ず確認する。
