# アーキテクチャ

> **注記:** [英語版](../architecture.md) が正式版です。
> この日本語版は参考訳です。
> 内容に差異がある場合は英語版を優先します。

## 目的

`agent-sessions` は、コーディングエージェントの対話記録を、プロバイダーに依存せず読み取り専用で参照するための層を提供します。

責務は次の範囲に限られます。

- ソースの検出
- プロバイダーへのアクセス
- 識別
- ソースのバージョンの観測
- 正規化
- 範囲を制限した出力
- 観測結果の完全性の報告
- 検証

下流の利用側が記録に与える解釈は扱いません。

## システム境界

```text
プロバイダーが保持する対話記録
────────────────────────────────────────────

Codex
├── 保存されたスレッドやセッションのデータ
└── 対応する読み取りインターフェース

Claude Code
└── 保存されたセッションの対話記録

ChatGPT
└── 利用者が要求した Data Export ZIP

                    │
                    ▼

agent-sessions
────────────────────────────────────────────

ソースの検出
プロバイダーアダプター
論理的な識別
バージョンの観測
正規化
範囲を制限した出力
完全性
検証

                    │
                    ▼

下流の利用側
────────────────────────────────────────────

ハーネスの振り返り
領域固有の統合
その他のローカルツール
```

| 主体 | 担当するもの |
| --- | --- |
| プロバイダー | 生の対話記録 |
| `agent-sessions` | 正規化されたアクセス仕様 |
| 下流の利用側 | 処理状態と解釈の状態 |

## アーキテクチャ上の不変条件

### 読み取り操作は変更を伴わない

確認を目的とする操作では、次のものを変更してはいけません。

- プロバイダーが保持する対話記録
- プロバイダーが保持するアプリケーションのメタデータ
- 利用側の状態
- 永続化された `agent-sessions` のアプリケーション状態

アダプターはこれらの値を書き込みません。\
プロバイダーのファイルを読み取ると、ホストのファイルシステムがアクセス時刻のメタデータを更新する場合があります。\
この副作用は、意味上の書き込み禁止の保証には含まれません。

プロバイダーの公式インターフェースを使えるのは、要求された操作での実際の動作がこの要件を守る場合だけです。

### 正しさに過去の実行を必要としない

ソースデータ、設定、ツールのバージョンが同じであれば、以前に `agent-sessions` を実行したかどうかにかかわらず、読み取り専用の操作は意味上同じ結果を返さなければなりません。\
ソースを正しく扱うために、レジストリの初期化や一覧作成の段階は必要ありません。

### 生の対話履歴を複製しない

`agent-sessions` は、プロバイダーの対話記録の完全な複製を永続化しません。\
プロバイダーの保持期間が終わった後も証拠を残す必要がある下流の利用側は、必要最小限の証拠の保持方針を自ら定めます。

### 利用側の状態を分離する

すべての利用側に共通する処理状態のモデルはありません。

同じバージョンのソースについて、次の組み合わせは有効です。

```text
利用側 A = レビュー済み
利用側 B = 未レビュー
```

アクセス層は、これらの状態を推定したり、整合させたりしてはいけません。

### 待機中の活動は既定でゼロ

このアーキテクチャは、次のいずれも必要としません。

- デーモン
- ファイル監視処理
- タイマー
- 定期的な同期処理
- ライフサイクルフックを使った取り込み処理

コマンドが動いていないとき、`agent-sessions` は処理を行いません。

## ドメインモデル

共通モデルでは、すべてのプロバイダーに `session` という用語を当てはめることを意図的に避けています。

### プロバイダー

対応する対話記録の生成元です。

実装済みのプロバイダーアダプターは次のとおりです。

```text
codex
claude
chatgpt
```

プロバイダー名はアクセス用のアダプターを識別するものであり、モデルの提供元を抽象的に表すものではありません。

### ソースインスタンス

個別に指定できる、プロバイダーの保存場所またはアクセス範囲です。

例を示します。

```text
codex-wsl
codex-windows-app
claude-wsl
```

ソースインスタンスには安定した論理 ID があり、一つのプロバイダーに属します。

プロバイダーによっては、次の情報も持ちます。

- プロバイダーのホームディレクトリ
- アーカイブのパス
- アクセス方法
- その他、そのマシン上の場所を示す情報

場所を示す情報は論理 ID を決めません。

### ソース

利用側が個別に指定できる、プロバイダーが保持する一つの対話記録です。

プロバイダー固有の代表的な形式は次のとおりです。

```text
Codex       → スレッドまたはセッション
Claude Code → セッション
ChatGPT     → エクスポートのスナップショット内の会話
```

論理的なソース ID には、少なくとも次の要素を含みます。

```text
プロバイダー
ソースインスタンス
プロバイダー固有のソース ID
```

安定した `v1` CLI は、次の形式で一意に決まる参照値を使います。

```text
as0:<provider>:<source-instance>:<source-id-fingerprint>
```

フィンガープリントは、プロバイダー固有のソース ID をドメイン分離して SHA-256 でハッシュ化した値です。\
これにより、コマンドラインで使う参照値に生の ID やマシン上の場所を含めずに済みます。

`as0` プレフィックスとソースのフィンガープリントに使う `v0` のドメイン分離子は、それぞれの形式とアルゴリズムのバージョンを表します。\
公開 JSON スキーマのバージョンとは独立しており、それぞれの意味とアルゴリズムが変わらない限り変更しません。

ファイルシステム上の場所はソース ID ではありません。

プロバイダーが記録を別の場所へ移動しても、プロバイダーが付与したソース ID が変わらなければ、それだけで新しい論理ソースにはなりません。

### ソースのやり取りの時刻

`last_interaction_at` は、一つの論理ソースに記録されたユーザーまたはアシスタントのメッセージ、ツール呼び出し、ツール結果のうち、確認できた最も遅い時刻です。\
アダプターは、上限内で読み取ったプロバイダーの記録から求め、UTC にそろえます。\
管理用の行、システム通知、ファイルの更新時刻は対象にしません。\
ChatGPT Data Export では、選択中の会話経路にあるやり取りを対象とします。

最終時刻を安全に確定できない場合、このフィールドはありません。\
その場合、`list` または `show` は省略を報告し、`complete` にはなりません。\
一部が欠けた一覧からは、対象期間の全件を確定できません。\
利用側は `list` の全ページを集め、絶対日時で絞り、時刻の降順と `source_ref` の昇順で並べられます。\
CLI の `source_ref` によるページ分割順は変わりません。

### 対話

ソースが表す論理的な会話または作業上の対話です。\
実装済みの Codex と Claude Code では、一つのソースが一つの対話に対応します。

### イベント

対話に含まれる、順序のある正規化された観測結果です。

共通のイベントモデルは次を表します。

- 利用者のメッセージ
- アシスタントのメッセージ
- ツール呼び出し
- ツールの結果
- エラー
- 実質的に利用できる場合のソース間の関係とライフサイクルのメタデータ

安全かつ忠実に表現できないプロバイダー固有の情報は、プロバイダーのメタデータとして残すか、省略したことを報告できます。\
正規化では、実質的に異なるプロバイダーの概念を同等のものとして扱ってはいけません。

## ソースの識別子と内容のバージョン

論理的な識別子と、観測した内容の版は別のものです。\
例えばセッションを再開した場合のように、プロバイダー固有の記録は同じ識別子を保ったまま変化することがあります。

### 版の手がかり

記録の一覧では、記録が変化した可能性を示すバージョンヒントを出します。\
多くのヒントは低コストのメタデータを使いますが、Codex の継承履歴では、やり取りの時刻を求めるために読み取った採用範囲のバイト列をハッシュ化します。

プロバイダーによっては、次のような手がかりを利用できます。

- プロバイダーが報告する更新時刻。
- 成果物の変更時刻。
- 成果物のサイズ。
- 現在有効な履歴が複数の成果物にまたがる場合に採用する内容。
- その他の安定したプロバイダーのメタデータ値。

バージョンヒントは処理を効率化するための手がかりです。\
永続的な暗号学的証拠ではありません。

各プロバイダーが、バージョンヒントの意味と独立した版を持つアルゴリズムの名前空間を管理します。\
Codex は現在の成果物の統計情報に `v0`、採用した継承履歴または切り出した履歴の内容と、それを解釈するために必要なロールアウトヘッダーに `v3` を使います。\
公開 JSON スキーマのバージョンを変更しても、ほかに変更がなければ、同じバージョンヒント自体は変わりません。

### 検証済みの版

利用側が後から確認できる出所の証拠を必要とする場合、検証操作で必要なソースの内容を読み取り、内容ハッシュまたは同等の検証済みバージョン識別子を返すことがあります。

利用側は、`agent-sessions` 自体にレビュー状態を保持させることなく、次の情報を保持できます。

```text
ソース参照
検証済みソースバージョン
利用側固有の判断
```

検証済みバージョンのハッシュには、専用の `v0` ドメイン分離子と `provider-content-v0` という基準を使います。\
これらの値は `schema_version` から独立しており、ハッシュ化の意味またはアルゴリズムが変わった場合にだけ変化します。

### 利用側の増分処理

下流の利用側は、次のように増分レビューを実装できます。

```text
ソースを一覧表示
      │
      ▼
バージョンヒントを
利用側が管理する状態と比較
      │
      ├── 変更なし
      │      └── 高コストなソースの読み取りを省略
      │
      └── 変更の可能性あり
             │
             ▼
           検証
             │
             ▼
       正規化したイベントを読み取り
```

したがって、増分処理に中央の処理済みセッション用データベースは必要ありません。

## プロバイダーへのアクセス

各プロバイダーアダプターは、公開された読み取り仕様を満たす、最も限定的なアクセス経路を選択します。

すべての本番アダプターは [ADR 0003](../decisions/0003-separate-provider-discovery-from-version-sensitive-decoding.md) を実装します。\
文書化されたプロバイダーのルート、対話記録の場所、エクスポートの取得経路で検出方法を定め、プロバイダーが管理する JSON の形式はアダプター内部でバージョンに依存するデコーダーへの入力として扱います。

### 公式インターフェースとファイルシステムへのアクセス

文書化されたプロバイダーのインターフェースが次を提供する場合は、それを優先します。

- 記録を十分に検出できること。
- 記録の内容を十分に保持できること。
- 上限を設けたアクセス。
- 変更を伴わない読み取り経路。

公式であることだけでは十分ではありません。

公式の操作がメタデータの修復、移行、同期、その他の永続的な変更を行う場合、アダプターは代わりに、より安全で対応済みのアクセス経路を選択できます。

より厳格な読み取り専用の要件を守るために必要であれば、ファイルシステム上のデータを直接解析する方法を代替経路として使用できます。

ファイルを読み取るアダプターは、サイズなどの上限を設けた通常ファイルだけを開きます。\
ディレクトリを読み取るアダプターは、開いたファイルの同一性、解決後のパスが指定範囲内にあること、プロバイダーのルートの同一性を再確認します。\
ファイルを直接指定するアダプターは、選択したファイルの同一性と内容のスナップショットを再確認します。\
特殊ファイルと、設定したソースの範囲外へのパスの差し替えを拒否します。

プロバイダー内部の形式は版に依存する入力であり、自動的に公開 `agent-sessions` 仕様へ昇格させません。

### Codex

Codex アダプターは文書化された Codex ホームを受け付け、その `sessions` と `archived_sessions` 配下のロールアウト成果物を検出します。\
読み取り専用の調査で Codex App Server を起動しません。

ユーザー設定は内部のセッションディレクトリではなく Codex ホームを識別します。

複数の Codex ホームまたは保存場所は、別々の名前付きソースインスタンスで表します。

検出ではサイズ制限内のロールアウトヘッダーだけを読み取り、`events` と `verify` は別途管理するバージョン依存のデコード境界を越えます。\
受け入れる成果物と行の互換性に関する証拠、正規化の選択、明示的な制限は [Codex プロバイダー](codex.md) に記載しています。

### Claude Code

Claude Code アダプターは文書化された設定ルートを受け付け、その `projects` 配下の主セッションのトランスクリプトを検出します。\
Claude Code を起動したりプロバイダーのデータやメタデータを変更したりせず、プロバイダーが管理する成果物を直接読み取ります。

独立して保存された複数の Claude Code ルートは、別々の名前付きソースインスタンスで表します。

明示的な `agent-sessions` 設定がない場合、プロバイダー設定の環境変数と文書化された既定値からソースの場所を得ることがあります。

検出では文書化された場所の仕様を使い、パスではなくトランスクリプトのプロバイダー所有の `sessionId` から論理識別子を取得します。\
JSONL の行と内容ブロックの形式は、別途管理するバージョン依存のデコード境界として残ります。

文書化されたサブエージェントのトランスクリプトの場所とバージョン固有のメタデータサイドカーは、プロバイダーのルート内で認識し、検証します。\
検証した記録が共通の識別子仕様を満たすプロバイダー所有のサブエージェント識別子を提供しないため、独立したソースとしては公開しません。

受け入れる場所の証拠、バージョン固有の記録の互換性、正規化の選択、制限は [Claude Code プロバイダー](claude.md) に記載しています。

### ChatGPT Data Export

ChatGPT アダプターは明示的に選択した Data Export ZIP だけを受け付けます。\
アーカイブを検出したり、エクスポートを要求・ダウンロードしたり、非公開の ChatGPT API を使ったりしません。

名前付きソースインスタンスは選択したアーカイブの境界を識別し、アーカイブ内の各会話は独立して指定できる論理ソースです。\
会話の識別子はプロバイダー固有の会話識別子から得ます。\
アーカイブの SHA-256 は別のスナップショットのバージョンヒントであり、同じ ZIP 内で見つかった会話が共有します。

アダプターは、アーカイブ、メンバー、圧縮率、入れ子、会話、イベントに明示した上限の範囲で、該当する会話 JSON メンバーをメモリ上で読み取ります。\
アーカイブメンバーをディスクに展開することはありません。\
アクティブな会話の分岐は、現在のノードから親リンクをたどり、時系列順に正規化します。

OpenAI は利用者が Data Export を取得する方法と、アーカイブにチャット履歴が含まれることを文書化していますが、内部の会話 JSON グラフを安定した API として公開していません。\
そのグラフはアダプター内に限定した互換性境界として残り、 [ChatGPT Data Export プロバイダー](chatgpt.md) に記載しています。

## 設定モデル

通常の単一ソース環境は専用の設定ファイルなしで動作します。

明示的な設定は次の用途に使います。

- 一つのプロバイダーにある複数の保存場所。
- 標準ではないプロバイダーのホーム。
- マウントしたプロバイダーのデータ。

解決順序は次のとおりです。

```text
1. 明示的な CLI オプション
2. agent-sessions のユーザー設定
3. プロバイダーの環境変数
4. プロバイダーの既定値
```

### プロバイダー単位の場所

設定は可能な限りプロバイダー単位の保存場所またはアクセス境界を識別します。

特定の `sessions/` サブディレクトリなど、プロバイダー内部のパスを固定するより、プロバイダー単位の概念を優先します。

例を挙げます。

```text
Codex のホーム
Claude Code の設定ルート
ChatGPT Data Export ZIP
```

内部レイアウトの知識はアダプターが持ちます。

### マシン固有の設定

ソースの場所にはマシン固有のパスやマウントポイントが含まれることがあります。\
そのため、持ち運ぶリポジトリ内のファイルではなく、マシン固有の設定に置きます。

任意の `v1` 設定は、`os.UserConfigDir` が返すディレクトリ配下の `agent-sessions/config.json` に置く厳密な JSON です。\
`id`、`provider`、絶対パスで示すプロバイダー単位の `root` フィールドを持つ、名前付きの `sources` が含まれます。

### 名前付きソースインスタンス

一つのプロバイダーに複数のインスタンスを置くことができます。

実装済みの設定は、これらの概念を厳密な JSON で表します。

```json
{
  "schema_version": "v1",
  "sources": [
    {"id": "codex-one", "provider": "codex", "root": "/fictional/codex-one"},
    {"id": "codex-two", "provider": "codex", "root": "/fictional/codex-two"}
  ]
}
```

場所を示す値は架空の例です。

## ステートレス性と保存

`agent-sessions` は意味上ステートレスです。

実装は次を必要としません。

- SQLite。
- その他の永続データベース。
- セッション登録簿。
- トランスクリプトのミラー。
- 永続検索インデックス。
- バックグラウンドイベントの一時保管。

これは実装上の近道ではなく、アーキテクチャ上の選択です。

### 破棄可能なキャッシュ

キャッシュを導入できるのは、現実的な測定により直接の検出または解析が実質的に高コストだと示された場合だけです。

将来のキャッシュは、次のすべてを満たさなければなりません。

- 正式なデータ源ではない。
- 正しさを失わずに削除できる。
- キャッシュがないことで変わるのは性能だけである。
- 変化のない読み取り操作が不要な永続書き込みを発生させない。
- 書き込み増幅を測定する。
- 待機中のディスク書き込みをゼロに保つ。

キャッシュは実装詳細にとどまり、利用側に公開する状態モデルの一部ではありません。

## リソースの動作

ディスクの書き込み耐久性は主要な品質属性です。

既定のリソース仕様は次のとおりです。

```text
待機中:
  バックグラウンド処理 = 0
  永続的な書き込み = 0

読み取り専用コマンド:
  agent-sessions の永続的な書き込み = 0

ソースへのアクセス:
  要求された操作に必要な範囲だけを読み取る
  より低コストなメタデータでソースを絞れる場合は履歴全体を読まない
```

Codex の `list` は参照先から採用する先頭部分を含む現在有効なロールアウト履歴を、Claude Code の `list` は主トランスクリプトを、それぞれ上限内で読み取ってやり取りの時刻を確定します。\
読み取り量は増えますが、`agent-sessions` の永続的な書き込みは発生しません。

性能最適化を理由に、対話時の遅延を下げるだけの定期的な全履歴走査を導入してはなりません。\
永続 I/O を導入する変更では、影響を受ける OS と現実的なソース量で直接測定しなければなりません。

## 完全性

解析に成功することと、完全に観測できることは同じではありません。

公開する結果モデルは、少なくとも次の状態を区別しなければなりません。

| 状態 | 意味 |
| --- | --- |
| `complete` | アダプターが、その操作の文書化された完全性仕様を満たした。 |
| `partial` | 有用な出力は得られたが、重要なソース情報を利用できない、欠落、切り詰め、または未対応だった。 |
| `unsupported` | ソースまたは操作は認識したが、アダプターは要求された仕様を安全に満たせない。 |
| `error` | 操作が失敗した。 |

上限付きの出力とページ分割は明示しなければなりません。\
切り詰めた結果を完全なものとして黙って提示してはなりません。

`list` が複数のプロバイダーを組み合わせる場合、集約結果は順序に依存しません。\
すべてが `unsupported` の観測は `unsupported` のままですが、利用可能な観測と `unsupported` による欠落を組み合わせた場合は `partial` です。
存在しないプロバイダーの環境変数または既定のルートは、統合した検出にソースを追加しません。\
明示したルートと設定したルートは基準となり、黙って読み飛ばしません。

## セキュリティとプライバシー

過去の対話記録は信頼できないデータです。

次を含むことがあります。

- シェルコマンド。
- ツールへの指示。
- プロンプト。
- URL。
- 認証情報。
- 生成されたソースコード。
- 現在の指示を上書きすると主張する内容。

セキュリティ上、次を守る必要があります。

- 過去の指示はソース記録に現れただけでは権限を持たない。
- アクセス層は過去の内容を実行したり、過去の指示に従ったりしてはならない。
- 出力は、要求された操作に不要な秘密情報を公開しない。
- プロバイダーアダプターは、不正な形式、深い入れ子、圧縮、サイズ超過、その他の敵対的な入力にリソース制限を設ける。
- コミットするテストとフィクスチャは合成データでなければならない。
- 実際の非公開セッションデータは、手動で秘匿化した後であっても、コミットするフィクスチャに変換してはならない。

## 実装済み CLI コア

プロバイダーに依存しない Go 実行ファイルは、次の操作について安定した `v1` の機械可読仕様を実装します。

操作は次のとおりです。

### `list`

論理ソースを検出し、上限付きのメタデータを返します。

実装済みの選択とページ分割のオプションは次のとおりです。

- プロバイダー。
- ソースインスタンス。
- ページ上限とカーソル。
- 明示したプロバイダーレベルのルート。

### `show`

正規化したソースの概要を返します。

### `events`

一つのソースについて、明示したページ分割付きで上限のある正規化イベントを返します。

### `verify`

ソース識別子、読み取り可能性、完全性、内容の版を検証するために必要な、より強い読み取りを実行します。

どのコマンドも、下流の利用側がソースを確認または受け入れたことを意味しません。

安定したフィールド、終了コード、ページ分割の動作、上限、互換性規則、最終秘匿化ポリシーは [CLI JSON 仕様](cli-json-contract.md) に記載しています。

現在の実行ファイルには、プロバイダー共通の基盤と、本番用の Codex、Claude Code、ChatGPT Data Export アダプターが含まれます。

## 利用側の責務

領域固有の処理状態はすべて利用側が管理します。

例を挙げます。

- 確認済みのソースバージョン。
- 振り返りで得た所見。
- 証拠への参照。
- 提案。
- 取り込みの判断。
- ドメイン固有の派生記録。

`agent-sessions` は証拠を公開します。\
その証拠に意味を割り当てません。

## 対象外

このアーキテクチャは現在、次を提供しません。

- 汎用エージェントメモリシステム。
- ハーネスの振り返りまたは改善。
- 個人メモリの意味付け。
- 正式なトランスクリプトアーカイブ。
- マシン間の自動同期。
- バックグラウンドテレメトリ。
- Web UI。
- ベクトル検索。
- クラウドストレージ。
- プロバイダーを変更する操作。

具体的な必要性が生じた場合は、別の利用側が読み取り仕様の上にこのような機能を構築できます。
